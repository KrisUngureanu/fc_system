package kz.tamur.comps.ui.ext;

/**
 * Реализует различные виды заголовков вкладок 
 */
public enum TabStretchType {

    /** Заголовок по размеру текста. */
    never,
    /** Многострочный заголовок. */
    multiline,
    /** Заголовки растягивать по размеру панели. */
    always
}